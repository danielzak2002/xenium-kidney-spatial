# ============================================================
# Raw data — NEVER commit (large 10x Xenium output bundles)
# ============================================================
kidney_10x/data/
kidney_10x_preview/data/

# Danaher et al. 2024 cLN CosMx cohort — entire data/ folder, recursively
# (the .RData objects AND the authors' analysis files); keep readme.txt tracked.
Danaher24/data/

# Dumoulin et al. 2026 DKD spatial atlas (Zenodo 19868428) — raw data/ folder
# (the ~8.7 GB .h5ad and Diagnosis.xlsx); keep readme.txt + ASSESSMENT.md tracked.
Demoulin26/data/

# Large binary formats anywhere in the tree (belt-and-suspenders)
*.ome.tif
*.tif
*.parquet
*.zarr
*.zarr.zip
*.zarr/
*.h5
*.h5ad
*.rds
*.mtx
*.mtx.gz
*.RData
*.rda

# Large generated objects (regenerate from scripts; document in README)
outputs/objects/

# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
env/
*.egg-info/

# ============================================================
# R / RStudio
# ============================================================
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# renv: keep renv.lock under version control, ignore the local library
renv/library/
renv/staging/
renv/python/

# ============================================================
# OS / editors
# ============================================================
.DS_Store
.vscode/
.idea/

# ============================================================
# Secrets — never commit
# ============================================================
.env
*.key
*.pem

# Cached annotation references (downloaded; see DATA.md)
refs/
analysis/three_cohort_integration/cells_labeled.parquet
analysis/three_cohort_integration/cells_labeled_reliable.parquet
presentation/figures/integration_walkthrough/model.parquet
